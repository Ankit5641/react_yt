// var h1 = document.createElement("h1");

// h1.innerHTML = "Hello World";
// document.body.appendChild(h1);
// console.log(h1);

//  there are two types of export in js ie named export and default export..
// in named export we can export multiple things and we have to import them with the same name as we exported them. 
// but in default export we can export only one thing and we can import it with any name we want.

// import arr from "./app.js";
// console.log(arr);